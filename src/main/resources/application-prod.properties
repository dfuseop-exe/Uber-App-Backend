spring.application.name=uberApp

spring.datasource.url=jdbc:postgresql://${DB_HOST_URL}/${DB_NAME}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.jpa.hibernate.ddl-auto=update

# Spatial Types Configuration
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

jwt.secretKey=${JWT_SECRETE_KEY}

#smtp config
spring.mail.username=${SMTP_USERNAME}sushants2418@gmail.com
spring.mail.password=${SMTP_PASSWORD}
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

